<%= turbo_stream.remove dom_id(@note) %>

<% video = @youtube_video || @video %>
<% if video.notes.reload.where(is_visible: true).empty? %>
  <%= turbo_stream.update "notes_list" do %>
    <p id="no_notes_message">メモがありません。</p>
  <% end %>
<% end %>
