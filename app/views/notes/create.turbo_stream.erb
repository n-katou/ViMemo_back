<%= turbo_stream.append "notes_list", partial: "notes/note", locals: { note: @note } %>

<% if @video.notes.reload.where(is_visible: true).count == 1 %>
  <%= turbo_stream.remove "no_notes_message" %>
<% end %>
